import React from 'react'

const LessonSection: React.FC = () => (
  <div className="h-full w-full flex items-center justify-center text-gray-400">
    Lesson Section (No content yet)
  </div>
)

export default LessonSection
