import React from 'react'

const QuestionSection: React.FC = () => (
  <div className="h-full w-full flex items-center justify-center text-gray-400">
    Question Section (No content yet)
  </div>
)

export default QuestionSection
